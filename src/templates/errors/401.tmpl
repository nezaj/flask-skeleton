{% extends "shared/layout.tmpl" %}

{% block title %}Unauthorized{% endblock %}

{% block content %}
<div class="jumbotron">
    <div class="text-center">
        <h1>Unauthorized</h1>
        <p>You are not authorized to see this page. Please <a href="{{ url_for('public.index')}}">log in</a> or
            <a href="{{ url_for('auth.register') }}">create a new account</a>.
        </p>
    </div>
</div>
{% endblock %}
